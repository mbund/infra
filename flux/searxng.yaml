apiVersion: v1
kind: Namespace

metadata:
  name: searxng
  labels:
    pod-security.kubernetes.io/enforce: privileged
---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization

metadata:
  name: searxng
  namespace: flux-system

spec:
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: "./workloads/searxng"
  interval: 60m
  timeout: 3m
  retryInterval: 2m
  prune: true
  wait: true
